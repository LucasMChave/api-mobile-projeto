import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        width: "80%",
        backgroundColor: "#222222",
        color: "#ffd6ff",
        borderRadius: 8,
        padding: 10,
        textAlign: "center",
        fontSize: 18,
        marginBottom: 20,
    },
});