import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    button: {
        width: "60%",
        height: 65,
        backgroundColor: "#4f4b8cff",
        justifyContent: "center",
        alignItems: "center",
        marginBottom: 20,
        borderRadius: 100,
        borderWidth: 3,
        borderColor: "#ffffff"
    },

    title: {
        color: "#ffffffff",
        fontSize: 25,
    },
});